@model List<Aplicacion.Administracion.Dto.ComoVoyDto>

<div class="container">
    <div class="row">
        @foreach (var n in @Model)
        {
            n.HTML = n.HTML.Replace("@", n.TEXTO);
            @Html.Raw(n.HTML);
        }
    </div>
</div>

